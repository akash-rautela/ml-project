requirements: 
  pandas
  numpy
  matplotlib
  sklearn
  streamlit
  pickle

what to do: 
  clone the directory
  open the app.py file
  run the following code in the terminal: "streamlit run app.py" to see the webpage application

  or 
  if you want to see the jupyter notebook, just open it.

all the datasets used are provided in the repo

now the project will open in the browser
